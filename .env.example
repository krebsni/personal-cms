# Frontend Development
VITE_API_URL=http://localhost:8787
VITE_WS_URL=ws://localhost:8787

# Backend (Cloudflare Workers - use wrangler secret put for production)
JWT_SECRET=your-256-bit-secret-key-for-development-only
ENVIRONMENT=development

# Database (D1 configuration is in wrangler.toml)
# R2 Storage (configuration is in wrangler.toml)
